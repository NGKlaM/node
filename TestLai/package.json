{
  "name": "testlai",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon --experimental-specifier-resolution=node ./app.js",
    "dev": "concurrently \"json-server -w db.json --port 3000\" \"nodemon --experimental-specifier-resolution=node ./app.js\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "expess": "^0.0.1-security",
    "express": "^4.18.2",
    "mongoose": "^8.0.0"
  }
}
